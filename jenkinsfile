pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                script {
                    sh './mvnw test' // Assuming Maven is used
                }
            }
        }
        stage('Build Maven'){
            steps{
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/arijknani/mongo-demo.git']]])
                sh 'mvn clean install'
            }
        }
    }
}

